
public class Exer3 {
	/*
	 * 3.Dados los siguientes valores para las variables booleanas a, b y c ( a =
	 * true, b = false y c = true), evaluar las expresiones que aparecen a
	 * continuación: 
	 * a) a && b || a && c--> verdadero y falso o verdedero y verdadero 
	 * b) (a || ! b) && (! a || c) -->verdadero o verdadero y falso o verdadero
	 * c) a || b && c -->verdadero o falso y verdadero
	 * d) ! (a || b) && c-->falso o verdadero
	 * 
	 * 
	 */
}
}
